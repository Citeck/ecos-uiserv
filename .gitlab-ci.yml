stages:
  - mirror

mirror-master:
  tags: 
    - gitlab-runner
  stage: mirror
  cache: {}
  script:
    - git remote add sync git@github.com:Citeck/ecos-uiserv.git
    - git checkout develop
    - git pull
    - git push sync develop
                        
